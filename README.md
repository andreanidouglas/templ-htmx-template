# Templ + HTMX simple template

This is a simple template to start with HTMX with Go and the templ template engine.

This also uses tailwindcss to generate all the css.

# Build

```bash
$ go mod tidy
$ make
```

